# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
out
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets and env (API keys, tokens — never commit)
.env
.env.*
!.env.example

# User/app data (lives in Electron userData, not in repo)
*.db
*.db-wal
*.db-shm
keychain.enc
**/secure/*.enc

# External skill/code (optional — remove if you want to track it)
.claude/

# Optional provider bundles (distribute separately; do not commit)
electron/main/cloud/optional-providers-dist/

# Cursor IDE
.cursor/
